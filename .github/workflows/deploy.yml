name: Deploy Torgide

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy-api:
    uses: ./.github/workflows/deploy-api.yml
    secrets: inherit

  deploy-web:
    needs: deploy-api
    uses: ./.github/workflows/deploy-web-app.yml
    secrets: inherit
